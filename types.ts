export interface NavItem {
  label: string;
  path: string;
}

export enum ServiceType {
  AI_ADVISORY = 'AI Advisory',
  TENDER_PACKS = 'Domestic Tender Packs',
  BUILT_ENV = 'Built Environment Advisory',
}